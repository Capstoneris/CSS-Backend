package de.hsh.capstoneris.data.factories;

public class GroupFactory {
    // public GroupDTO getGroupById(long id)
    // public GroupDTO getGroupByTitle(String title)

    // public void saveOrUpdateGroup(GroupDTO group)    // make sure to remove or add new entries in users_in_groups table!
    // public void deleteGroup(GroupDTO group)          // make sure to remove entries in users_in_groups table!
}
